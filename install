sudo apt-get install udisks
#พอใช้แล้ว login ไม่ได้อีกเลย ubuntu มัน access home ไม่ได้
#sudo wget "https://winpath.googlecode.com/hg/50-winpath.rules" -O "/etc/udev/rules.d/50-winpath.rules"
#sudo chmod +x "/etc/udev/rules.d/50-winpath.rules"
sudo wget "https://winpath.googlecode.com/hg/winpath.py" -O "/usr/bin/winpath.py"
sudo chmod +x "/usr/bin/winpath.py"
sudo wget "https://winpath.googlecode.com/hg/winpath" -O "/usr/bin/winpath"
sudo chmod +x "/usr/bin/winpath"
sudo wget "https://winpath.googlecode.com/hg/winpath.png" -O "/usr/share/icons/winpath48.png"
sudo wget "https://winpath.googlecode.com/hg/winpath48.png" -O "/usr/share/icons/winpath48.png"
sudo mkdir "/computer"
sudo chmod 777 -R "/computer"
winpath
